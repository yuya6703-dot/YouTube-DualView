/**
 * src/lib/features.ts
 * ---------------------------------------------------------------
 * 表示を止めている機能のスイッチ。
 *
 * ★ 2026-09-17 ユーザー判断で「次に再生」キューとタイムスタンプメモを非表示にした。
 *   削除ではなく非表示にしたのは、メモが自分で書いたデータ（chrome.storage.local の
 *   `notes`）であることと、将来また欲しくなったときの復元コストをゼロにするため。
 *   コード・i18n・保存データ（`queue` / `notes`）はそのまま残しており、
 *   ここを true に戻せばそのまま復活する。
 *
 * ★ キューを隠すときは自動連続再生も必ず止めること（popout.tsx 側で同じフラグを見る）。
 *   保存済みのキューが残っていると、見えないキューが動画終了時にページ遷移を起こす。
 * ---------------------------------------------------------------
 */
export const FEATURES: { queue: boolean; notes: boolean } = {
  // 「次に再生」キュー: 関連動画の「＋」ボタン・キューパネル・自動連続再生・
  // オプション画面の「キューの自動連続再生」設定
  queue: false,
  // タイムスタンプメモ: サブ画面のメモパネル
  notes: false
}
